
  <h3>Public Trip</h3>
  <small>Visiting: <%= trip.escape('location') %>
    (<%= trip.escape('arrival_date') %> to <%= trip.escape('departure_date') %>)
  </small>

  <p>Description: <%= trip.escape('description') %></p>

<form class="content-form group">
  <button  data-id="<%= trip.id %>" id="edit_trip"><i class="fa fa-pencil"></i></button>
</form>
